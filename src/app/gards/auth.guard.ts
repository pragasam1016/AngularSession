import { CanActivateFn } from '@angular/router';

export const authGuard: CanActivateFn = (route, state) => {
  // create a API 
    // get user details
        // can i access this route
          //If api returns true
          debugger;
  return false;
          // else false -- route to access dend comp
};
